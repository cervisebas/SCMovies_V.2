import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SCMovies_V.2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
